import { SearchAlbumPipe } from './search-album.pipe';

describe('SearchAlbumPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAlbumPipe();
    expect(pipe).toBeTruthy();
  });
});
